#!/usr/bin/node
// Write a script that imports an array and computes a new array.
console.log(const Old = require('./100-data.js').list);
console.log(const NewAray = Newaray.map((Value1, Value2) => Value1 * Value2));
